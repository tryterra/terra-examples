# Terra API credentials — from https://dashboard.tryterra.co → API keys.
# These stay server-side only (used by server/index.ts to mint streaming
# tokens); they are never sent to the browser.
TERRA_DEV_ID=
TERRA_API_KEY=

# Port for the local token server (the Vite dev proxy points here).
# PORT=4000
